<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <!--导入属性配置 -->
    <properties resource="generatorConfig.properties"/>

    <context id="context" targetRuntime="MyBatis3">
        <plugin type="com.pickle.utils.mbg.MyGeneratorPlugin">
            <!-- 项目目录 -->
            <property name="targetProject" value="${manage-models}\src\main\java"/>
            <property name="resourcesProject" value="${manage-models}\src\main\resources"/>
            <!--要生成的地址-->
            <property name="controllerPackage" value="com.pickle.${manage-package}.controller"/>
            <property name="servicePackage" value="com.pickle.${manage-package}.service"/>
            <property name="serviceImplPackage" value="com.pickle.${manage-package}.service.impl"/>
            <property name="mapperPackage" value="com.pickle.${manage-package}.mapper"/>
            <property name="xmlPackage" value="mapper.${manage-package}"/>
            <!--父类接口-->
            <property name="superController" value="com.pickle.utils.base.BaseController"/>
            <property name="superServiceInterface" value="com.pickle.utils.base.IBaseService"/>
            <property name="superServiceImpl" value="com.pickle.utils.base.BaseService"/>
            <property name="superMapper" value="com.pickle.utils.base.BaseMapper"/>

            <property name="generatorUserName" value="${generator-user-name}"/>
            <property name="flFlag" value="${fl-flag}"/>
        </plugin>

        <!--数据库链接地址账号密码-->
        <jdbcConnection driverClass="${jdbc-connection-drive-class}"
                        connectionURL="${jdbc-connection-url}"
                        userId="${jdbc-connection-username}"
                        password="${jdbc-connection-password}">
            <property name="nullCatalogMeansCurrent" value="true"/>
            <property name="useInformationSchema" value="true"/>
        </jdbcConnection>

        <!--生成Model类存放位置-->
        <!--targetPackage就是包名，targetProject需要到除最顶层的项目名称外的第二级model，在此处就是到start模块-->
        <javaModelGenerator targetPackage="com.pickle.${manage-package}.bean" targetProject="${manage-models}\src\main\java">
            <!--是否允许生成子包，即targetPackage.schemaName.tableName,否则直接在下面生成entity-->
            <property name="enableSubPackages" value="true"/>
            <!--是否添加构造方法，这里采用了lombok，所以不需要-->
            <property name="constructorBased" value="false"/>
            <!--在get方法中，对String字符串调用trim()方法-->
            <property name="trimStrings" value="false"/>
            <!--继承某一个类-->
            <property name="rootClass" value="${bean-root-class}"/>
        </javaModelGenerator>

        <!--生成映射文件存放位置-->
        <sqlMapGenerator targetPackage="mapper" targetProject="${manage-models}\src\main\resources">
            <property name="enableSubPackages" value="false"/>
        </sqlMapGenerator>

        <!--生成Dao类存放位置-->
        <javaClientGenerator type="XMLMAPPER" targetPackage="com.pickle.${manage-package}.mapper" targetProject="${manage-models}\src\main\java">
            <property name="enableSubPackages" value="false"/>
        </javaClientGenerator>

        <!--指定需要生成对应表及类名
             domainObjectName="" 会自动生成类名称-->
        <table tableName="${table-name}"
               enableCountByExample="false"
               enableUpdateByExample="false"
               enableDeleteByExample="false"
               enableSelectByExample="false"
               selectByExampleQueryId="false">
            <!--是否使用数据库真实的字段名，为false将自动转驼峰-->
            <property name="useActualColumnNames" value="false"/>
            <ignoreColumn column="ZF_BZ"/>
        </table>
    </context>
</generatorConfiguration>